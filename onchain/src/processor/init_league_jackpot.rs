use solana_program::{
    account_info::AccountInfo, 
    entrypoint::ProgramResult,
    pubkey::Pubkey,
};

pub fn init_league_jackpot(
    league_name: String,
    accounts: &[AccountInfo],
    program_id: &Pubkey
) -> ProgramResult {
    
    
    Ok(())
}